syntax = "proto3";

// single frame of jpeg video stream from camera, with timestamp
message CameraFrame {
  uint64 timestamp_ns = 1;
  bytes jpeg_data = 2;
  uint32 width = 3;
  uint32 height = 4;
}

